name: errordeck-cli
version: 0.1.0

authors:
  - Håkan <hakan@dun.se>

dependencies:
  dotenv:
    github: gdotdesign/cr-dotenv
    version: "~> 0.3.0"

targets:
  errordeck-cli:
    main: src/errordeck-cli.cr

crystal: 1.5.0

license: MIT
